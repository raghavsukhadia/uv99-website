# backgrounds

Reserved for full-bleed page background images.

Note: `styles.css` (`.about-uv99__bg-img`) already references
`/assets/images/backgrounds/about-bg-car.jpg`, but that file does not exist
anywhere in the project — this was already the case before the reorg
(the rule has a graceful CSS fallback colour). Add the real file here if/when
it's available.
